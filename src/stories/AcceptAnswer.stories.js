import React from 'react';
import { AcceptAnswer } from './AcceptAnswer';

export default {
    title : "Accept Answer component",
    Component: AcceptAnswer,
}

const Template = args => <AcceptAnswer {...args}/>

export const Default = Template.bind({})